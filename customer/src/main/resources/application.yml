server:
  port: 8080

spring:
  application:
    name: customer

  datasource:
    url: jdbc:postgresql://10.22.21.240:5431/customer
    username: amigoscode
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
